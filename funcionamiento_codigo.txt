Dibujito avion:
Para comenzar creo una lista en la que introduzco todos los asientos del avion clasificados en letra y numero. 
En esta lista asigno en el primer bucle for a cada fila un numero, y en el segundo el asiento con su letra. Con f''{fila}{sitio} creo lo que seria el numero de asiento, cada fila (numero) con su sitio (letra), de esta forma tengo todos los asientos del avion.

La segunda lista contiene todos los asientos asignados, para ello use la funcion .tolist() que me convierte los datos de las columnas 'Asiento' de los dos csv en una lista unica.

Hago un bucle que valla de 1 a 21,numero las filas y hago dos bucles mas,uno para el bloque 'ABC' y otro 'DEF. En estos bucles busco que si el asiento esta en la lista de asientos reservados escriba un R, y si no lo esta que ponga una L


Porcentaje del vuelo ocupado:
Para ello calculo el numero de filas en cada archivo, las sumo y las divido entr 120, luego lo multiplico por 100 y tengo el porcentaje de ocupacion.

Sabemos cuantos pasajeros hay por el numero de filas de los archivos, tambien sabemos que el avion tienen 120 asientos.
Numero de pasajeros alojados en cada clase:
Para realizar esta parte de la rpactica primero use la funcion 'data.shape' para contar el nuemro de filas de cada archivo csv, con el numero de filas tengo el numero de pasajeros que ocupa cada clase. 

Numero de pasajeros que facturan su maleta:
Creo una funcion que recorra la columna de facturacion de cada archivo y si se encuntra con que el dato empeiza por S, de 'si' lo contabilice. Añado el comando 'strip' para eleimiar los espacios en balnco por is hubiese alguno y 'lower' las letras las paso a minusculas.
Luego creo otra funcion que sume las maeltas facturadas en cada clase y ya tengo el total

Para las funciones del menu:
- Informacion acerca de las reservas: en funcion de la clase elgida se muestra un csv u otro.
- Informacion pasajeros:

- Añadir reserva: primero necesito saber si se tratara de un nuevo pasajero de business o turista. Para ello le doy a elegir entre dos opciones, en funcion de la que eliga, la informacion de la nueva reserva, que se pide a continuacion se añade al csv correspondiente. Tambien añadi un mensaje de confirmacion de que la reserva se añadio correctamente.
- Modificar reserva
- Eliminar reserva: repeti la estrcutra de que lo priemro que se haga sea elegir en que clase esta el pasajero y despues decidi que para eliminar la reserva, que seria equivalente a eliminar la fila del csv, se introduejses el dni asociado a esa reserva. 
